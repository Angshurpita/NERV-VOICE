# EchoSphere Backend API & Agora Conversational AI Agent Configuration
# Copy to apps/api/.env and configure with your credentials. Never commit the real file.

PORT=3001
NODE_ENV=development

# Signs session cookies and authenticates internal CustomLLM callbacks.
# Generate with: openssl rand -base64 48
AUTH_SECRET=

# ── Public URL & CustomLLM Endpoint ──────────────────────────────────────────
# PUBLIC_URL: The public HTTPS base URL of this backend (e.g. via Cloudflare tunnel, ngrok, or production domain).
# AGORA_LLM_URL: The complete public CustomLLM endpoint URL.
# If AGORA_LLM_URL is set, it is used exactly as supplied.
# If AGORA_LLM_URL is not set but PUBLIC_URL is provided, the system constructs:
#   ${PUBLIC_URL}/api/agora/openai/v1/chat/completions
#
# IMPORTANT: Agora Cloud Service runs in the cloud and CANNOT reach localhost or private LAN IPs.
# For local testing, expose port 3001 using a tunnel (e.g., cloudflared tunnel --url http://localhost:3001)
# Example:
#   PUBLIC_URL=https://voice.example.com
#   AGORA_LLM_URL=https://voice.example.com/api/agora/openai/v1/chat/completions
PUBLIC_URL=
AGORA_LLM_URL=

# Neon PostgreSQL connection string. Leave blank to run in memory for quick demos.
# DATABASE_URL=postgresql://user:pass@ep-xxx.aws.neon.tech/neondb?sslmode=require

# Origins allowed to send credentialed requests (CORS).
CORS_ORIGINS=http://localhost:3000,http://localhost:5174

# ── Gemini Reasoning & Decision Brain ─────────────────────────────────────────
# Gemini powers conversational phrasing inside EchoSphere's deterministic runTurn().
# NEVER expose this key to frontend or browser!
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash

# ── Speech recognition (Agora Ares) & synthesis (OpenAI TTS) ──────────────────
# The Cloud Agent transcribes with Agora's built-in Ares ASR and speaks with
# OpenAI TTS, so no separate STT vendor key is needed here.
AGORA_STT_MODEL=ares
OPENAI_API_KEY=
OPENAI_TTS_MODEL=openai-tts
OPENAI_TTS_VOICE=sage

# ── Agora Conversational AI & RTC ─────────────────────────────────────────────
# Required for Agora RTC audio transport and Cloud Conversational AI Agent.
AGORA_APP_ID=
AGORA_APP_CERTIFICATE=
AGORA_CUSTOMER_ID=
AGORA_CUSTOMER_SECRET=
AGORA_RTC_TOKEN_TTL=3600

# ── First-run admin ───────────────────────────────────────────────────────────
SEED_ADMIN=true
SEED_ADMIN_EMAIL=admin@echosphere.dev
SEED_ADMIN_PASSWORD=Echosphere123
SEED_ADMIN_NAME=Ops Admin

# ── Escalation Policy ─────────────────────────────────────────────────────────
HUMAN_REQUESTS_BEFORE_HANDOVER=3
CONFIDENCE_HIGH=0.85
CONFIDENCE_MEDIUM=0.60
ORDER_ID_CONFIRMATION_THRESHOLD=0.85
IDENTITY_CONFIRMATION_THRESHOLD=0.85
HIGH_VALUE_INR=25000
